<Window x:Class="Wizzy.Pages.Tools.AllTools.TImer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wizzy.Pages.Tools.AllTools"
        mc:Ignorable="d"
        Title="TImer" Height="391" Width="263">
    <Grid>
        <StackPanel Margin="0,10,0,0" VerticalAlignment="Top" HorizontalAlignment="Center" Width="119">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label
                    x:Name="Sec"
                    Content="00"
                    FontSize="23"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Grid.Column="0" Grid.ColumnSpan="2" Width="37"/>
                <Label
                    Content=":"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    FontSize="23"
                    Grid.Column="1" 
                    Width="14" Height="40"/>
                <Label
                    x:Name="MiliSec"
                    Content="00"
                    FontSize="23"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Grid.Column="2" 
                    Width="40"/>
            </Grid>
            <StackPanel>
                <Button
                Content="Start"
                Click="StartButtont_Click"
                Margin="10"
                />
                <Button
                    Content="Resume"
                    Click="ResumeButtont_Click"
                    Margin="10"
                    />
                <Button
                     Content="Stop"
                    Click="StopButtont_Click"
                    Margin="10"
                    />
            </StackPanel>
            
        </StackPanel>
    </Grid>
</Window>
